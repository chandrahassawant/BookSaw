@* <header id="header">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-2">
                <div class="main-logo">
                    <a href="index.html">
                        <img src="~/booksaw-1.0.0/booksaw-1.0.0/images/main-logo.png" alt="logo">
                    </a>
                </div>
            </div>
            <div class="col-md-10">
                <nav id="navbar" class="navbar navbar-expand-lg navbar-custom">
                    <div class="container-fluid d-flex justify-content-between align-items-center">
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <a asp-area="" asp-controller="Home" asp-action="Index" class="nav-link">Home</a>
                            </li>
                            @if (User.IsInRole("Admin"))
                            {
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                                        aria-expanded="false">
                                        Managements
                                    </a>

                                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                        <li><a class="dropdown-item" href="@Url.Action("GetAllUsers", "User")">Users
                                                Managements</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("Index", "Client")">Publications</a>
                                        </li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("GetStock", "Product")">Stocks</a>
                                        </li>
                                    </ul>
                                </li>
                            }
                            else if (User.IsInRole("Employeer"))
                            {
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                                        aria-expanded="false">
                                        Managements
                                    </a>

                                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                        <li><a class="dropdown-item" href="@Url.Action("GetAllUsers", "User")">Users
                                                Managements</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("Index", "Client")">Publications</a>
                                        </li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("GetStock", "Product")">Stocks</a>
                                        </li>
                                    </ul>
                                </li>
                            }
                            else if (User.IsInRole("Seller"))
                            {
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                                        aria-expanded="false">
                                        Managements
                                    </a>

                                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                        <li><a class="dropdown-item" href="@Url.Action("GetStock", "Product")">Stocks
                                                Managements</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("OrdersReceived", "Order")">View
                                                Orders</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("GetStock", "Product")">Others</a>
                                        </li>
                                    </ul>
                                </li>
                            }
                             else if (User.IsInRole("Customer"))
                            {
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                                        aria-expanded="false">
                                        Orders
                                    </a>

                                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                        <li><a class="dropdown-item" href="@Url.Action("GetOrders", "Order")">My
                                                Orders</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("Index", "CustomerAddress")">Manage
                                                Address</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("GetStock", "Product")">Returns</a>
                                        </li>
                                    </ul>
                                </li>
                            }
                            <li class="nav-item">
                                <a href="#featured-books" class="nav-link">Featured</a>
                            </li>
                            <li class="nav-item">
                                <a href="#popular-books" class="nav-link">Popular</a>
                            </li>
                            <li class="nav-item">
                                <a asp-area="" asp-controller="Product" asp-action="Index" class="nav-link">Product</a>
                            </li>
                            <li
                            <ul class="navbar-nav" style="height: 50px;">
                                @using Microsoft.AspNetCore.Identity
                                @using BulkyWeb.Models
                                @inject SignInManager<User> SignInManager
                                @inject UserManager<User> UserManager

                                @if (SignInManager.IsSignedIn(User))
                                {
                                    <li class="nav-item dropdown">
                                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                                            aria-expanded="false">
                                            Account
                                        </a>

                                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                            <li><a class="dropdown-item"
                                                    href="@Url.Action("GetAllUsers", "User")">Profile</a></li>
                                            <hr class="dropdown-divider">
                                            <li><a class="dropdown-item" href="@Url.Action("Index", "Client")">View
                                                    Orders</a></li>
                                            <hr class="dropdown-divider">
                                            <li class="list-group-item">
                                                <form id="logoutForm" asp-area="Identity" asp-page="/Account/Logout"
                                                    asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                                                    <a href="#"
                                                        onclick="document.getElementById('logoutForm').submit(); return false;"
                                                        class="dropdown-item">
                                                        Logout
                                                    </a>
                                                </form>
                                            </li>
                                        </ul>
                                    </li>
                                    <li class="nav-item">
                                        <span class="nav-link text-dark">Hello, @UserManager.GetUserName(User)!</span>
                                    </li>
                                }
                                else
                                {
                                    <div
                                        style="display: flex; justify-content: right center; align-items: center; height: 100vh;">
                                        <a class="nav-link btn btn-primary text-white" id="login" asp-area="Identity"
                                            asp-page="/Account/Login" style="margin: 560px; width: 100px;">Login</a>

                                    </div>
                                    <div
                                        style="display: flex; justify-content: left center; align-items: center; height: 100vh;">
                                        <a class="nav-link btn btn-primary text-white" id="register" asp-area="Identity"
                                            asp-page="/Account/Register" style="margin: -1950px;">Register</a>
                                    </div>

                                }
                            </ul>
                        </ul>
                    </div>
                </nav>
            </div>
        </div>
    </div>
</header> *@

<header id="header">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-2">
                <div class="main-logo">
                    <a href="index.html">
                        <img src="~/booksaw-1.0.0/booksaw-1.0.0/images/main-logo.png" alt="logo">
                    </a>
                </div>
            </div>
            <div class="col-md-10">
                <nav id="navbar" class="navbar navbar-expand-lg navbar-custom">
                    <div class="container-fluid d-flex justify-content-between align-items-center">
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <a asp-area="" asp-controller="Home" asp-action="Index" class="nav-link">Home</a>
                            </li>



                            @if (User.IsInRole("Admin"))
                            {
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                                        aria-expanded="false">
                                        Managements
                                    </a>

                                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                        <li><a class="dropdown-item" href="@Url.Action("GetAllUsers", "User")">Users
                                                Managements</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("Index", "Client")">Publications</a>
                                        </li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("GetStock", "Product")">Stocks</a>
                                        </li>
                                    </ul>
                                </li>
                            }
                            else if (User.IsInRole("Employeer"))
                            {
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                                        aria-expanded="false">
                                        Managements
                                    </a>

                                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                        <li><a class="dropdown-item" href="@Url.Action("GetAllUsers", "User")">Users
                                                Managements</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("Index", "Client")">Publications</a>
                                        </li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("GetStock", "Product")">Stocks</a>
                                        </li>
                                    </ul>
                                </li>
                            }
                            else if (User.IsInRole("Seller"))
                            {
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                                        aria-expanded="false">
                                        Managements
                                    </a>

                                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                        <li><a class="dropdown-item" href="@Url.Action("GetStock", "Product")">Stocks
                                                Managements</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("OrdersReceived", "Order")">View
                                                Orders</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("GetStock", "Product")">Others</a>
                                        </li>
                                    </ul>
                                </li>
                            }
                             else if (User.IsInRole("Customer"))
                            {
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                                        aria-expanded="false">
                                        Orders
                                    </a>

                                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                        <li><a class="dropdown-item" href="@Url.Action("GetOrders", "Order")">My
                                                Orders</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("Index", "CustomerAddress")">Manage
                                                Address</a></li>
                                        <hr class="dropdown-divider">
                                        <li><a class="dropdown-item" href="@Url.Action("GetStock", "Product")">Returns</a>
                                        </li>
                                    </ul>
                                </li>
                            }




                            <li class="nav-item">
                                <a href="#featured-books" class="nav-link">Featured</a>
                            </li>
                            <li class="nav-item">
                                <a href="#popular-books" class="nav-link">Popular</a>
                            </li>
                            <li class="nav-item">
                                <a asp-area="" asp-controller="Product" asp-action="Index" class="nav-link">Product</a>
                            </li>
                            <li
                            <ul class="navbar-nav" style="height: 50px;">
                                @using Microsoft.AspNetCore.Identity
                                @using BulkyWeb.Models
                                @inject SignInManager<User> SignInManager
                                @inject UserManager<User> UserManager

                                @if (SignInManager.IsSignedIn(User))
                                {
                                    <li class="nav-item dropdown">
                                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                                            aria-expanded="false">
                                            Account
                                        </a>

                                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                            <li><a class="dropdown-item"
                                                    href="@Url.Action("GetAllUsers", "User")">Profile</a></li>
                                            <hr class="dropdown-divider">
                                            <li><a class="dropdown-item" href="@Url.Action("Index", "Client")">View
                                                    Orders</a></li>
                                            <hr class="dropdown-divider">
                                            <li class="list-group-item">
                                                <form id="logoutForm" asp-area="Identity" asp-page="/Account/Logout"
                                                    asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                                                    <a href="#"
                                                        onclick="document.getElementById('logoutForm').submit(); return false;"
                                                        class="dropdown-item">
                                                        Logout
                                                    </a>
                                                </form>
                                            </li>


                                        </ul>
                                    </li>
                                    <li class="nav-item">
                                        <span class="nav-link text-dark">Hello, @UserManager.GetUserName(User)!</span>
                                    </li>


                                }
                                else
                                {
                                    <div
                                        style="display: flex; justify-content: right center; align-items: center; height: 100vh;">
                                        <a class="nav-link btn btn-primary text-white" id="login" asp-area="Identity"
                                            asp-page="/Account/Login" style="margin: 600px; width: 100px;">Login</a>

                                    </div>
                                    @* <div
                                        style="display: flex; justify-content: left center; align-items: center; height: 100vh;">
                                        <a class="nav-link btn btn-primary text-white" id="register" asp-area="Identity"
                                            asp-page="/Account/Register" style="margin: -2065px;">Register</a>
                                    </div> *@

                                }
                            </ul>




                        </ul>

                        <div class="hamburger">
                            <span class="bar"></span>
                            <span class="bar"></span>
                            <span class="bar"></span>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
    </div>
</header>
